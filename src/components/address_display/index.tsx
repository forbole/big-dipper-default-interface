import React from 'react';

/**
 * Displays address based on role without the avatar
 */
const AddressDisplay = () => {
  return (
    <div>address display</div>
  );
};

export default AddressDisplay;
