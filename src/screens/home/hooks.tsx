import { handleSearchbarSubmit } from '@utils';

export const useHomeHook = () => {
  // ==========================
  // search bar
  // ==========================

  return {
    handleSearchbarSubmit,
  };
};
